# 🚨 INSECURE: Port 22 (SSH) is open to the global internet - asdasdsadsa.
resource "aws_security_group" "insecure_sg" {
  name        = "insecure-sg"
  description = "Allow SSH from anywhere"

  ingress {
    from_port   = 22
    to_port     = 22
    protocol    = "tcp"
    cidr_blocks = ["0.0.0.0/0"] # This is the problem
  }
}
